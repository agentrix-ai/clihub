[project]
name = "clihub-web"
version = "0.1.0"
description = "clihub.cc — Landing page + Schema Registry API for cli-hub"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "jinja2>=3.1",
]
